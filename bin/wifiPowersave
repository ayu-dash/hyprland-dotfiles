#!/bin/bash

IFACE=$(iw dev 2>/dev/null | awk '$1=="Interface"{print $2; exit}')
[[ -z "$IFACE" ]] && exit 0

case "$1" in
    on)  iw dev "$IFACE" set power_save on  ;;
    off) iw dev "$IFACE" set power_save off ;;
    *)   echo "Usage: wifi-powersave {on|off}"; exit 1 ;;
esac
